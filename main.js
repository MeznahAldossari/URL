let input = document.getElementById("urlInput");

function firstSize(w, h) {
  url = input.value;

  //   window.open(url, "", "width=" + w + ", height=" + h);
  window.open(url, "", "width=" + w + ", height=" + h);
}
